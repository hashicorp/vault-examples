<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/vault-web-app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/vault-web-app.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Vault Secret Change Logger -->
    <appender name="VAULT_SECRET" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/vault-secret-changes.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/vault-secret-changes.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Database Logger -->
    <appender name="DATABASE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/database-operations.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/database-operations.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>5MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for Vault Secret Service -->
    <logger name="com.example.vaultweb.service.VaultSecretService" level="DEBUG" additivity="false">
        <appender-ref ref="VAULT_SECRET"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger for Database Service -->
    <logger name="com.example.vaultweb.service.DatabaseService" level="INFO" additivity="false">
        <appender-ref ref="DATABASE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Logger for Spring Cloud Vault -->
    <logger name="org.springframework.cloud.vault" level="DEBUG"/>
    <logger name="org.springframework.vault" level="DEBUG"/>

    <!-- Logger for Spring Boot -->
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>

    <!-- Logger for Hibernate -->
    <logger name="org.hibernate" level="WARN"/>

    <!-- Logger for Tomcat -->
    <logger name="org.apache.catalina" level="WARN"/>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Profile-specific configurations -->
    <springProfile name="dev">
        <logger name="com.example.vaultweb" level="DEBUG"/>
        <logger name="org.springframework.cloud.vault" level="DEBUG"/>
    </springProfile>

    <springProfile name="prod">
        <logger name="com.example.vaultweb" level="INFO"/>
        <logger name="org.springframework.cloud.vault" level="WARN"/>
    </springProfile>
</configuration>
